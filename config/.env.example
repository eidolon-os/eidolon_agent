# eidolon-agent secrets (copy to config/.env; replace placeholder values)
EIDOLON_AGENT_LLM_API_KEY=EIDOLON_AGENT_LLM_API_KEY
PAIRING_JWT_SECRET=PAIRING_JWT_SECRET
# Narrow service credential used only to resolve Owner-scoped Companion runtime facts.
EIDOLON_DATA_COMPANION_AUTHORITY_TOKEN=EIDOLON_DATA_COMPANION_AUTHORITY_TOKEN
