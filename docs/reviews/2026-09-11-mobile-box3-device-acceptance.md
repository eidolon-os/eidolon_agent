# Mobile / BOX-3 真机验收

范围：用户指定 Pi5 部署由另一任务负责；本任务只负责 Mobile 和 BOX-3。尚未开始真实通话，不以安装或启动成功替代体验验收。

## 设备准备

- 平板：`df331f93`，型号 `24091RPADC`，Android 16/API 36。
- Mobile：从 `d67e11e` 构建 debug APK，通过 `adb install -r` 安装成功，未卸载或清除应用数据。APK SHA-256：`1ed4f54f7fbf09e1448f13ddd8d670fe4f633ac31ba06fb92a110a3ba974ac84`。工作区原有设备路由评审文档修改保留。
- BOX-3：`/dev/cu.usbmodem1101`。使用官方 ESP-IDF 5.5.4 环境和 `eidolon_verify_flashed` 正常重启读取构建指纹；板上 `git=60f9d9ca4`、LiveKit SDK `312ce4742b35f92e47915a161d271f7ef027ce5a`、`idf=5.5.4` 与本地最后构建一致，构建时间 2026-09-10 22:50:47。因此未重新烧录，未擦除 NVS/配对/网络设置。

## 接下来逐步验收

1. 等待另一任务完成 Pi5 部署；平板打开 App、选择 Pi5，核对实际连接状态。
2. 先以平板为对话设备，验证短问答与正常结束。
3. 验证近期信息回忆、详略要求和设置生效。
4. 再接 BOX-3，核对 Ready、收音、播报与结束，逐项测 PTT/半双工/全双工。
5. 针对已知打断与长回答场景收集设备、客户端和服务端可关联的证据；用户反馈与日志共同判断，不把感知问题只凭服务状态判为通过。

本任务不执行 Pi5 部署、重启或配置变更。主机版本和就绪由部署任务提供；尚未修改本轮自动化报告中的未完成项。
